# key_rotation/.env

# Key rotation timing and consensus settings
KEY_ROTATION_INTERVAL_DAYS=30        # How often to rotate keys (days)
CERT_VALIDITY_DAYS=365              # Certificate validity period (days)
VOTE_THRESHOLD_PERCENT=66           # % of votes needed for consensus
VOTE_TIMEOUT_HOURS=48               # How long proposals are valid (hours)

# Security settings
BACKUP_PASSWORD=your_secure_password_here  # Password for key backups (replace with a strong value)